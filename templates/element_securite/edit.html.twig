{% extends 'base.html.twig' %}

{% block title %}Modifier — {{ element_securite.nom }}{% endblock %}

{% block body %}
    <div class="page-header">
        <h1>✏️ Modifier — {{ element_securite.nom }}</h1>
        <div class="btn-group">
            <a href="{{ path('app_element_securite_show', {'id': element_securite.id}) }}" class="btn btn-outline">← Retour</a>
        </div>
    </div>

    {{ include('element_securite/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <div class="mt-2">
        {{ include('element_securite/_delete_form.html.twig') }}
    </div>
{% endblock %}
