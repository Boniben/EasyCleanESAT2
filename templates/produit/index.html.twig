{% from '_macros/action_buttons.html.twig' import action_buttons %}
{% extends 'base.html.twig' %}

{% block title %}Produit{% endblock %}

{% block body %}
    <div style="margin-bottom: 20px;">
        <h1 style="text-align:center; margin:0 0 12px 0;">Produits</h1>
        <div style="display:flex; justify-content:flex-end; gap:8px;">
            <a href="{{ path('app_produit_inactif') }}" class="btn" style="background:#ffd8a8;color:#7c2d12;border:1px solid #fdba74;font-weight:600;font-size:16px;padding:10px 20px;">Éléments désactivés</a>
            <a href="{{ path('app_produit_new') }}" class="btn btn-success" style="font-size: 16px; padding: 10px 20px;">+ Créer</a>
        </div>
    </div>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Code</th>
                <th>Nom</th>
                <th>Couleur</th>

                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for produit in produits %}
            <tr>
                <td>{{ produit.id }}</td>
                <td>{{ produit.code }}</td>
                <td>{{ produit.nom }}</td>
                <td>
                    {% set colorMap = {
                        'vert': '#22c55e',
                        'rouge': '#ef4444',
                        'bleu': '#3b82f6',
                        'jaune': '#ffff01',
                        'orange': '#fb923c'
                    } %}
                    {% set couleurKey = produit.couleur|lower %}
                    {% set bgColor = couleurKey starts with '#' ? couleurKey : (colorMap[couleurKey] ?? couleurKey) %}
                    <span style="display: inline-block; width: 24px; height: 24px; border-radius: 4px; background-color: {{ bgColor }}; border: 1px solid #ccc;" title="{{ produit.couleur }}"></span>
                </td>

                <td>


                    {{ action_buttons('app_produit_show', 'app_produit_edit', produit.id) }}


                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">Aucun enregistrement trouvé</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}
